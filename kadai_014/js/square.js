const squareValueByArrow = (num) => {
    console.log(num * num);
}

function squareValueByFunction(num) {
    console.log(num * num);
}

// アロー関数での実行
squareValueByArrow(2);

// 関数での実行
squareValueByFunction(2);
